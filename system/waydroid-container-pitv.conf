[Service]
# Raspberry Pi 4 HEVC/rpivid uses the stateless V4L2 Media Request API.
# Re-apply PiTV's /dev/media* Waydroid extension after package upgrades and
# before the Python container manager imports its helper modules.
ExecStartPre=/usr/local/libexec/pitv-waydroid-device-patch
